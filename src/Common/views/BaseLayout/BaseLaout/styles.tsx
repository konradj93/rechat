import { styled } from '@mui/material/styles';

export const FullPageContainer = styled('div')({
    minWidth: '100vw',
    minHeight: '100vh',
    display: 'flex',
    flexDirection: 'column',
    flexWrap: 'wrap',
  });
